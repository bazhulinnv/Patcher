#include <iostream>
#include <string>
#include <PatchBuilder/PatchBuilder.h>

using namespace std;

int main() 
{
	string patchListDirectory; // Directory of patch list - a text file containing all the objects that need to be included in the patch
	string objectListDirectory; // Directory of object list - text file containing all database objects
	string installPocketDirectory; // Directory of install pocket - directory containing all the files needed to install the patch

	cout << "Hello!";
}